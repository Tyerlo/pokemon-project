import {StyleSheet} from "react-native";

export const btnStyles = StyleSheet.create({
  btn: {
    fontSize: 20,
    borderRadius: 4,
    borderColor: "#FFFF",
    color: "#ffff",
    height: 35,
    backgroundColor: "#6D3B47"
  }
});
